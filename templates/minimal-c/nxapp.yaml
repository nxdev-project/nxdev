schemaVersion: 1

application:
  titleId: "{{PROJECT_TITLE_ID}}"
  name: "{{PROJECT_NAME}}"
  author: "{{PROJECT_AUTHOR}}"
  version: "{{PROJECT_VERSION}}"
  description: "Minimal Nintendo Switch application in C"

build:
  language: c
  standard: "c11"
  defaultProfile: debug
