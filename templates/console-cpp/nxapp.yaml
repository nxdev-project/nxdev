schemaVersion: 1

application:
  titleId: "{{PROJECT_TITLE_ID}}"
  name: "{{PROJECT_NAME}}"
  author: "{{PROJECT_AUTHOR}}"
  version: "{{PROJECT_VERSION}}"
  description: "Nintendo Switch application using NXDevSDK"

dependencies:
  - nxdev.core
  - nxdev.input

build:
  language: cpp
  standard: "c++20"
  defaultProfile: debug
