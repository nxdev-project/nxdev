# NXDev Package Registry

This directory contains metadata, index schemas, and package definitions for devkitPro portlibs and NXDev community packages.

## Registry Schema

Packages are registered with:
- Name and version
- Description and author
- Dependencies
- devkitPro package mappings (pacman package name)
- Portlib CMake integration hooks
